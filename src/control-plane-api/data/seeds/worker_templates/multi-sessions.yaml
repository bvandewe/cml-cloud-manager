# Worker Template: metal
# Metal worker with nested virtualization for large-scale deployments
#
# Per ADR-007: Templates are managed as configuration, not user-created entities.
# Note: Metal instances are expensive - use for production deployments only

id: worker-template-multi
name: multi-sessions
description: Multi-sessions worker for large-scale deployments
instance_type: m5zn.metal
ami_name_pattern: 'cisco-cml2.9-lablet-v0.1.8'
capacity:
    cpu_cores: 48
    memory_gb: 192
    storage_gb: 256
    max_nodes: 200
cost_per_hour_usd: 3.9641
enabled: true
created_by: seeder
