"""PodDefinition projection commands (Phase 2 / G-12).

These commands project SE-emitted CloudEvents
(``pod_definition.ready.v1`` / ``pod_definition.sync_failed.v1``) into the
CPA-owned ``pod_definitions_read`` collection.
"""
